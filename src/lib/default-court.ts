/**
 * Juzgado semilla del entorno demo (migración `tutelia_core`, trigger `handle_new_user`, Settings).
 * El valor operativo para listados y radicación debe salir del perfil vía `SessionCourtProvider` / `useSessionCourt`.
 */
export const DEFAULT_DEMO_COURT_ID = 'court-1';
